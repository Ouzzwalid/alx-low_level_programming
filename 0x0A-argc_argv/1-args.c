#include <stdio.h>
/**
 *main - prints the number if arguments passed to it
 *
 *@argc : the number of arguments including the program name
 *@argv : array of arfgs
 *
 *Return: 0
 */
int main(int argc, char *argv[])
{
	argv = argv;

	printf("%d\n", argc - 1);
	return (0);
}
