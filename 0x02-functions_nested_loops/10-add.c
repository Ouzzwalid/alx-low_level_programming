#include "main.h"
/**
 * add -  adds two integers and returns the result
 *
 * @a: The first number
 * @b: the second number
 *
 * Return: The sum of the two numbers
 */

int add(int a, int b)
{
	return (a + b);
}
